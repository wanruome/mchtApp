<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 填充的颜色：这里设置背景透明 -->
    <solid android:color="#EEF3FA" />
    <!-- 边框的颜色 ：不能和窗口背景色一样 -->

    <!-- 设置按钮的四个角为弧形 -->
    <!-- android:radius 弧形的半径 -->
    <corners
        android:bottomLeftRadius="@dimen/dpx010"
        android:bottomRightRadius="@dimen/dpx010"
        android:topLeftRadius="@dimen/dpx010"
        android:topRightRadius="@dimen/dpx010" />
    <!-- padding：Button里面的文字与Button边界的间隔 -->
  <padding
        android:bottom="@dimen/dpx016"
        android:left="@dimen/dpx016"
        android:right="@dimen/dpx016"
        android:top="@dimen/dpx016" />

</shape>